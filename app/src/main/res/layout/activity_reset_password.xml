<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorText"
    >

    <TextView
        android:id="@+id/textView2"
        android:layout_width="352dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dp"
        android:textColor="?attr/colorAltText"
        android:text="Looks like you have forgotten your password. Please type in your log-in email below and we will get you set up!"
        android:textAlignment="center" />

    <EditText
        android:id="@+id/email_text_field"
        android:layout_width="347dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="150dp"
        android:backgroundTint="?attr/colorHint"
        android:textColor = "?attr/colorAltText"
        android:hint="Email"
        android:textColorHint="?attr/colorHint"/>

    <Button
        android:id="@+id/forgotPswdBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="210dp"
        android:backgroundTint="?attr/colorAccent"
        android:textColor="?attr/colorText"
        android:text="Send an email" />
</RelativeLayout>